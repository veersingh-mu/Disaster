[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "B"]
ignore = ["E501", "B008"]

[tool.pytest.ini_options]
minversion = "7.0"
pythonpath = [".", "backend"]
testpaths = ["backend/tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"
